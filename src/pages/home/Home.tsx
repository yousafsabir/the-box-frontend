import React from "react";
import Navbar from "@components/navbar/Navbar";

const Home: React.FC = () => {
    return (
        <div>
            <Navbar />
        </div>
    );
};

export default Home;
